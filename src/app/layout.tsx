import type { Metadata } from "next";
import { Crete_Round, Inter, Noto_Sans } from "next/font/google";
import "./globals.css";
import { Providers } from "@/applications/Provider/provider";

const creteRound = Crete_Round({ subsets: ["latin"], weight: ["400"] });
const inter = Inter({
  subsets: ["latin"],
  weight: ["400", "700", "900"],
  variable: "--font-inter",
});
const notoSans = Noto_Sans({
  subsets: ["latin"],
  weight: ["400", "700", "900"],
  variable: "--font-notoSans",
});

export const metadata: Metadata = {
  title: "Chucky | Welcome To Chucky",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${creteRound.className} ${inter.className} ${notoSans.className} antialiased`}
      >
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
